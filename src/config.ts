// Below this many salons in the database, Home lists everyone directly instead of
// gating behind geolocation — Nearby only earns its keep once the directory is dense.
export const NEARBY_THRESHOLD = 15;
